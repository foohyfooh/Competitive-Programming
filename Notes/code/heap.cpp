// Use this if inserting at the end of a heap
template<typename T, typename F>
void shiftUp(T heap[N + 1], F cmp, T val){

}

// Use this if inserting at the front of a heap
template<typename T, typename F>
void shiftDown(T heap[N + 1], F cmp, T val){

}
